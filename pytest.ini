[pytest]
minversion = 8.0
testpaths = tests
addopts = -ra --strict-markers --cov=core --cov=trading --cov-report=term-missing --cov-config=.coveragerc --cov-fail-under=80
markers =
    unit: Unit tests with no network/external APIs.
    integration: Integration tests that may call external services.
